<%#
# Edit

This view is the template for the edit page.

It displays a header, and renders the `_form` partial to do the heavy lifting.

## Local variables:

- `page`:
  An instance of [Administrate::Page::Form][1].
  Contains helper methods to help display a form,
  and knows which attributes should be displayed in the resource's form.

[1]: http://www.rubydoc.info/gems/administrate/Administrate/Page/Form
%>

<% content_for(:title) { "#{t("administrate.actions.edit")} #{page.page_title}" } %>

<header class="main-content__header" role="banner">
  <h1 class="main-content__page-title">
    Edit lesson
  </h1>

  <div>
      <%= link_to(
        "Preview lesson",
        lesson_path(id: page.resource.slug, preview: true),
        class: "button",
        target: "_blank"
      ) if valid_action?(:show) && !page.resource.slug.nil? %>
  </div>
</header>

<section class="main-content__body"
    data-help-section="<%= page.resource_name %>"
    ng-controller="<%= angular_controller(page.resource_name) %>">
  <%= render "form", page: page %>
  <%= render 'admin/help/modal' %>
</section>
